# Copy to server/.env for local development

# ─── Database (Neon PostgreSQL) ───────────────────────────────
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# ─── Auth ───────────────────────────────────────────────────
JWT_SECRET="change-me-to-a-long-random-string-at-least-32-chars"

# Optional: your local React dev URL
CLIENT_ORIGIN="http://localhost:3000"

# ─── Supabase Storage (images) ──────────────────────────────
# Project Settings → API in Supabase dashboard
SUPABASE_URL="https://xxxxxxxx.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-never-expose-in-frontend"
SUPABASE_STORAGE_BUCKET="media"
SUPABASE_STORAGE_FOLDER="uploads"

# ─── First-time seed (change password after deploy) ─────────
SEED_ADMIN_EMAIL="admin@dreaminternationaltours.com"
SEED_ADMIN_PASSWORD="ChangeThisPassword123!"
SEED_ADMIN_NAME="Dream Admin"

# ─── Email (Resend) ─────────────────────────────────────────
# API key: https://resend.com/api-keys
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxx"

# Sender — use onboarding@resend.dev for testing; use your verified domain in production
# Example: "Dream International <noreply@dreaminternationaltours.com>"
RESEND_FROM_EMAIL="Dream International <onboarding@resend.dev>"

# Where admin notifications are delivered (defaults to CMS contactEmail setting)
RESEND_ADMIN_EMAIL="info@dreaminternationaltours.com"
