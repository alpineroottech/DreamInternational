{
  "installCommand": "npm install && npm install --prefix server && npm run prisma:generate --prefix server && (node scripts/prisma-migrate-deploy-retry.mjs || echo \"Prisma migrate skipped due to DB connectivity\") && node scripts/merge-server-modules.mjs",
  "buildCommand": "CI=false npm run build:web",
  "outputDirectory": "build",
  "functions": {
    "api/[...all].mjs": {
      "maxDuration": 26
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/[...all]"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
